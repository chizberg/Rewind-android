# Committed placeholder secrets so the project builds without the real keys (CI / fresh clone).
# Put the REAL values in local.properties (gitignored) — see README / M4.
#
# MAPS_API_KEY        key A: Maps SDK for Android + Places (New), restricted by app signature.
# GOOGLE_REST_API_KEY key B: REST APIs (Street View metadata, Cloud Translation), API-restricted.
MAPS_API_KEY=REPLACE_ME_LOCALLY
GOOGLE_REST_API_KEY=REPLACE_ME_LOCALLY
